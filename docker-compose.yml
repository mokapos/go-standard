version: '2'
services:
  database:
    image: 'postgres:9.6-alpine'
    ports:
      - '5432:5432'
    environment:
      POSTGRES_PASSWORD: 'password'
      POSTGRES_USER: 'mokapos'
      POSTGRES_DB: 'moka-golang'
